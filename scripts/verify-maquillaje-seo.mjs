import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
const source=process.argv[2]||new URL('../maquillaje-novia-puebla.html',import.meta.url);
async function load(input){if(typeof input==='string'&&/^https?:\/\//.test(input)){const response=await fetch(input,{redirect:'follow'});if(response.status!==200)throw new Error(`HTTP ${response.status}`);return{html:await response.text(),status:response.status,source:input}}const path=input instanceof URL?fileURLToPath(input):input;return{html:await readFile(path,'utf8'),status:200,source:path}}
const clean=(value='')=>value.replace(/<[^>]*>/g,' ').replace(/&amp;/g,'&').replace(/\s+/g,' ').trim();const one=(html,re,label)=>{const match=html.match(re);if(!match)throw new Error(`Falta ${label}`);return clean(match[1])};const assert=(condition,message)=>{if(!condition)throw new Error(message)};
const {html,status,source:loaded}=await load(source);const title=one(html,/<title>([\s\S]*?)<\/title>/i,'title');const description=one(html,/<meta\s+name=["']description["']\s+content=["']([^"']*)/i,'description');const canonical=one(html,/<link\s+rel=["']canonical["']\s+href=["']([^"']*)/i,'canonical');const robots=one(html,/<meta\s+name=["']robots["']\s+content=["']([^"']*)/i,'robots');const h1=[...html.matchAll(/<h1\b[^>]*>([\s\S]*?)<\/h1>/gi)].map(match=>clean(match[1]));
assert(status===200,'La página no devuelve 200');assert(title==='Maquillaje para novia en Puebla | Shanty Nachon Stylist','Cambió el title congelado');assert(description==='Maquillaje para novia en Puebla con opción de peinado, prueba y atención en Puebla, Angelópolis, Cholula y Atlixco. Consulta disponibilidad.','Cambió la description congelada');assert(canonical==='https://shantynachon.com/maquillaje-novia-puebla','Cambió el canonical');assert(robots==='index, follow','Cambió robots');assert(h1.length===1&&h1[0]==='Maquillaje para novia en Puebla','Cambió el H1 o existe más de uno');
for(const type of ['Service','BreadcrumbList','FAQPage','ImageObject'])assert(html.includes(`"@type":"${type}"`),`Falta schema ${type}`);for(const href of ['/','/peinado-novia-puebla','/maquillaje-social-puebla','/maquillaje-y-peinado-angelopolis'])assert(html.includes(`href="${href}"`),`Falta enlace ${href}`);for(const phrase of ['El maquillaje de novia no se elige aislado','Puede trabajarse un acabado natural, luminoso, elegante o más definido','Se revisa disponibilidad para Angelópolis, Cholula y Atlixco'])assert(clean(html).includes(phrase),`Falta contenido sustantivo: ${phrase}`);
const images=[...html.matchAll(/<img\b[^>]*>/gi)].map(match=>match[0]);assert(images.length>=4,'Faltan demostraciones visuales');assert(images.every(image=>/\bwidth=["']\d+/.test(image)&&/\bheight=["']\d+/.test(image)&&/\balt=["'][^"']+/.test(image)),'Hay imágenes sin dimensiones o alt');assert(images.filter(image=>/fetchpriority=["']high/.test(image)).length===1,'Debe existir una sola imagen prioritaria');console.log(JSON.stringify({source:loaded,status,title,description,canonical,robots,h1:h1[0],h1Count:h1.length,images:images.length,result:'PASS'},null,2));
